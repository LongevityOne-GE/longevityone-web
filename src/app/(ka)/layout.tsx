import { SiteLayout } from '@/components/marketing/SiteLayout'

export default function KaLayout({ children }: { children: React.ReactNode }) {
  return <SiteLayout lang="ka">{children}</SiteLayout>
}
